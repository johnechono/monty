#include "monty.h"

/**
 * nop - will do nothing
 * @line_count: no count
 * @stack: it does not matter now
 * Return: NOTHING or NULL
 */
void nop(stack_t **stack, unsigned int line_count)
{
	(void) stack;
	(void) line_count;
}
